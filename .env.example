# API Configuration
# In development: uses json-server on localhost:4000
# In production: uses Next.js API routes (/api)
# Override with custom URL if needed:
# NEXT_PUBLIC_API_URL=http://localhost:4000
