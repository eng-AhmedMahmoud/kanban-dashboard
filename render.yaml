services:
  - type: web
    name: kanban-api
    env: node
    buildCommand: npm install
    startCommand: node api-server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
